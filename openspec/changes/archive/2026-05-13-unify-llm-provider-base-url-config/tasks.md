## 1. Унификация контракта конфигурации

- [x] 1.1 Обновить общий LLM-контракт так, чтобы каждый поддерживаемый провайдер настраивался через единообразные env-параметры `api key`, `model` и `base URL`
- [x] 1.2 Убрать префикс `` из provider-specific параметров `model` и `base URL`, сохранив `LLM_PROVIDER` как общий переключатель активного провайдера
- [x] 1.3 Убрать special-case вокруг OpenAI `base URL` и запретить hidden hardcoded endpoint как целевое runtime-поведение

## 2. Согласование runtime и документации

- [x] 2.1 Обновить runtime-конфиг, системную диагностику и шаблон `desengine.config-example.txt` под единый паттерн `<PROVIDER>_MODEL` и `<PROVIDER>_BASE_URL` без префикса ``
- [x] 2.2 Согласовать `docs/openai.md`, `docs/deepseek.md`, `docs/gemini.md` и связанные root-инструкции с новым единым контрактом

## 3. Проверка и регрессия

- [x] 3.1 Добавить проверку, что активный провайдер читает `model` и `base URL` только из ожидаемой env-конфигурации, а не из legacy-имён или захардкоженного адреса
- [x] 3.2 Зафиксировать локальную команду запуска проверки этого change без дополнительных credentials и mock-данных
