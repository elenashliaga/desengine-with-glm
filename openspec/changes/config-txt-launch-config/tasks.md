## 1. Локальный конфиг и шаблон

- [x] 1.1 Перевести проект с `.env.local` на `config.txt` как на канонический локальный конфиг-файл
- [x] 1.2 Добавить или переименовать шаблон в `config-example.txt`
- [x] 1.3 Обновить содержимое шаблона: оставить заполненными `DESENGINE_OPENAI_MODEL`, `DESENGINE_ALLOWLIST_BASE_URL` и `DESENGINE_ALLOWLIST_SALT`, очистить только `OPENAI_API_KEY`
- [x] 1.4 Добавить в первой строке шаблона комментарий о переименовании файла в `config.txt`
- [x] 1.5 Добавить короткий комментарий рядом с `OPENAI_API_KEY`, где взять ключ OpenAI

## 2. Bootstrap и диагностика

- [x] 2.1 Ввести единый загрузчик `config.txt` для runtime и вспомогательных сценариев
- [x] 2.2 Сохранить приоритет прямых переменных окружения над значениями из `config.txt`
- [x] 2.3 Обновить smoke-check, диагностические сообщения и вспомогательные скрипты под новый путь к конфигу

## 3. Документация

- [x] 3.1 Обновить root-документацию (`README.md`, `INSTALL.md`) под `config.txt` и `config-example.txt`
- [x] 3.2 Обновить документацию внутри `lab` (`README.md`, `env/openai.md`, `env/access-control.md` и связанные материалы)
- [x] 3.3 Проверить, что в документации и пользовательских подсказках больше не осталось ссылок на `.env.local` и `.env.local.example`
