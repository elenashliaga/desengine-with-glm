# Навигация

Навигация определяет официальный публичный URL-контракт продукта и карту пользовательских entry point'ов.

## Purpose

Зафиксировать человекочитаемые path-based адреса продукта и убрать query-driven пользовательскую навигацию из официального контура.

## Requirements

### Requirement: Продукт имеет фиксированную top-level карту пользовательских URL

Система SHALL предоставлять пользователю следующие канонические top-level адреса:
- `/`
- `/tasks`
- `/levels`
- `/auth`
- `/config`
- `/help`

#### Scenario: Пользователь ориентируется в основных разделах продукта
- **WHEN** пользователь открывает или копирует основной адрес продукта
- **THEN** top-level URL отражают структуру разделов продукта без скрытых экранов в query-параметрах

### Requirement: Корневой маршрут открывает публичную страницу состояния системы

Система SHALL использовать `/` как публичную точку входа со страницей состояния системы, а не как redirect на список задач.

#### Scenario: Пользователь открывает корень продукта до допуска
- **WHEN** пользователь открывает `/`
- **THEN** система показывает публичную страницу состояния
- **AND** не перенаправляет пользователя на `/tasks`

#### Scenario: Пользователь уже получил допуск и снова открывает корень продукта
- **WHEN** пользователь с валидным allowlist-допуском открывает `/`
- **THEN** система всё равно показывает публичную страницу состояния
- **AND** список задач остаётся доступен на `/tasks`

### Requirement: Пользовательские URL не используют query-параметры как официальный контракт

Система SHALL не использовать адреса вида `?param=1&q=2` как часть официальной пользовательской навигации.

#### Scenario: Пользователь переходит по публичной ссылке на экран продукта
- **WHEN** пользователь открывает человекоориентированный URL продукта
- **THEN** адрес кодирует экран через path-сегменты
- **AND** не требует query-параметров для определения пользовательского экрана

### Requirement: Legacy-path следующего уровня не обязан быть самостоятельным экраном

Система SHALL трактовать маршрут `/tasks/<taskId>/next` как допустимую точку входа для перехода к следующему уровню, но не обязана использовать его как самостоятельный пользовательский экран.

#### Scenario: Пользователь открывает legacy-path следующего уровня
- **WHEN** пользователь открывает `/tasks/<taskId>/next`
- **THEN** система может перенаправить его на канонический адрес задачи `/tasks/<taskId>`

### Requirement: Полное завершение задачи имеет отдельный path внутри задачи

Система SHALL использовать отдельный маршрут `/tasks/<taskId>/done` для экрана полного завершения задачи.

#### Scenario: Пользователь завершил последний уровень задачи
- **WHEN** пользователь попадает на экран полного завершения задачи
- **THEN** система открывает канонический адрес `/tasks/<taskId>/done`

### Requirement: Страницы config и help существуют как отдельные top-level маршруты

Система SHALL предоставлять отдельные top-level страницы `/config` и `/help`.

#### Scenario: Пользователь открывает страницу конфигурации
- **WHEN** пользователь открывает `/config`
- **THEN** система показывает страницу конфигурации и статусов подключения
- **AND** навигационные элементы этого интерфейса используют канонические framework-компоненты маршрутизации в соответствии с `ui-foundation`

#### Scenario: Пользователь открывает страницу помощи
- **WHEN** пользователь открывает `/help`
- **THEN** система показывает отдельную страницу помощи
- **AND** навигационные элементы этого интерфейса используют канонические framework-компоненты маршрутизации в соответствии с `ui-foundation`
