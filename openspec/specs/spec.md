# AI трансформатор (desengine)

Инструмент, который помогает превращать **картинку (PNG) интерфейсного компонента** в **набор файлов React‑компонента** (код + истории Storybook), через итеративную работу с LLM.

Этот файл — “входная вики‑страница” спецификаций: здесь только обзор, термины и ссылки на более маленькие спеки.

## Статус и ближайшая цель

Ближайшая цель: поддерживать работающий MVP с **настраиваемым LLM-провайдером**:
- OpenAI по ключу;
- Ollama по URL на локальной машине или в локальной сети.

## Карта спецификаций

- Задание (Task): см. `openspec/specs/task/spec.md`.
- Уровни задач: см. `openspec/specs/task-levels/spec.md`.
- Набор файлов компонента: см. `openspec/specs/component-file-set/spec.md`.
- Итерация (LLM‑запуск): см. `openspec/specs/iteration/spec.md`.
- Слепок (публичная публикация): см. `openspec/specs/snapshot/spec.md`.
- Пользователь и прогресс: см. `openspec/specs/user-progress/spec.md`.
- Подключение LLM: см. `openspec/specs/llm/spec.md`.
- Локальный endpoint LLM: см. `openspec/specs/llm-endpoint/spec.md`.

## Термины (кратко)

- **Задание (Task)** — PNG + набор файлов компонента.
- **Уровень задачи (Task Level)** — глобальный уровень обучения, на котором пользователь проходит ту же самую задачу.
- **Верстак (Workbench)** — место, где открыто одно задание и выполняются итерации.
- **Итерация (Iteration)** — один запуск LLM по промпту пользователя.
- **Промпты** — пользовательские + системные (производственные/дидактические).
- **Слепок (Snapshot)** — публично публикуемый пакет: итоговые файлы + история промптов (без секретов).

## Общее описание системы (вики-уровень)

1. Пользователь выбирает задание (PNG обязателен).
2. Система ведёт итерации: промпт пользователя + история промптов + выбранные вводные файлы → обновлённые файлы набора.
3. После каждой итерации пользователь видит картинку, текущие файлы и результат рендера.
4. Пользователь может опубликовать слепок (файлы + промпты).
